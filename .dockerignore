# 빌드 컨텍스트(Docker로 전송되는 파일 목록)에서 제외.
# 여기 있는 것들은 이미지 빌드에 필요 없거나, 이미 로컬에만 있어야 하는 파일들.

.git
.gradle
.idea
.vscode
build
out
bin

*.md
docs

# 로컬 개발 중 실제로 업로드된 파일 (이미지에 넣을 이유 없음)
uploads

# 비밀값은 반드시 런타임에 주입, 이미지에 절대 포함하지 않음
.env
.env.*
!.env.example
